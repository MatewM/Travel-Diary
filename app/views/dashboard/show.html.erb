<%# ============================================================
    DASHBOARD — TaxDays
    El navbar vive en layouts/application.html.erb
    ============================================================ %>
<%# Subscribe to per-user ticket updates broadcast from ParseTicketJob %>
<%= turbo_stream_from "tickets_#{current_user.id}" %>
<%# Invisible frame for process_tickets form response %>
<%= turbo_frame_tag "process_tickets_frame", class: "hidden" %>

<div class="min-h-screen bg-slate-50 relative overflow-hidden" data-controller="modal" data-modal-target="container">

  <%# === FONDO: logo con opacidad baja, no compite con el contenido === %>
  <div class="absolute top-0 left-0 z-0 pointer-events-none opacity-10">
    <%= image_tag "logo-expats.png", class: "w-[70vw] max-w-[900px] translate-x-0 translate-y-8" %>
  </div>

  <%# === CONTENIDO PRINCIPAL === %>
  <div class="relative z-10 max-w-5xl mx-auto px-4 sm:px-6 py-8">

    <%# --- 1. HEADER DE PÁGINA: Saludo + Botones --- %>
    <header class="flex flex-col sm:flex-row sm:items-end justify-between mb-8 gap-4">

      <%# IZQUIERDA: título y subtítulo %>
      <div>
        <h1 class="text-2xl font-bold text-slate-900">
          Hola, <%= current_user.name&.split(' ')&.first || current_user.email.split("@").first %> 👋
        </h1>
        <p class="mt-1 text-slate-500 text-sm">
          Gestiona tus viajes y controla tus días de residencia fiscal.
        </p>
      </div>

      <%# DERECHA: botón PDF + botón Subir %>
      <div class="flex gap-3 items-center">
        <button class="bg-white border border-slate-300 text-slate-700
                       hover:bg-slate-50 px-4 py-2 rounded-lg text-sm font-medium
                       transition shadow-sm whitespace-nowrap">
          Generar Informe PDF
        </button>

        <%= link_to new_ticket_path,
              data: { turbo_frame: "modal" },
              class: "bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-lg
                      shadow-md shadow-indigo-200 flex items-center gap-2 text-sm font-medium
                      transition-colors whitespace-nowrap" do %>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"/>
          </svg>
          Subir Billetes
        <% end %>
      </div>

    </header>

    <%# --- 2. CARDS DE RESUMEN --- %>
    <!-- PLACEHOLDER: reemplazar con datos reales en siguiente fase -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-8">

      <%# Card 1 — Residencia Principal (verde) %>
      <div class="bg-white rounded-xl border border-slate-200 shadow-sm p-5">
        <div class="flex items-center justify-between mb-3">
          <span class="text-xs font-bold text-slate-400 uppercase tracking-wider">Residencia Principal</span>
          <span class="text-xs font-semibold bg-emerald-100 text-emerald-700 px-2 py-0.5 rounded-full">En Curso</span>
        </div>
        <div class="flex items-center gap-2 mb-3">
          <span class="text-2xl">🇨🇾</span>
          <span class="text-base font-semibold text-slate-800">Chipre</span>
        </div>
        <div class="flex items-center justify-between text-xs text-slate-400 mb-1.5">
          <span>0 días acumulados</span>
          <span>183 días objetivo</span>
        </div>
        <div class="w-full bg-slate-100 rounded-full h-1.5">
          <div class="bg-emerald-500 h-1.5 rounded-full" style="width: 0%"></div>
        </div>
      </div>

      <%# Card 2 — Riesgo Fiscal (ámbar) %>
      <div class="bg-white rounded-xl border border-slate-200 shadow-sm p-5">
        <div class="flex items-center justify-between mb-3">
          <span class="text-xs font-bold text-slate-400 uppercase tracking-wider">Riesgo Fiscal</span>
          <span class="text-xs font-semibold bg-amber-100 text-amber-700 px-2 py-0.5 rounded-full">Cuidado</span>
        </div>
        <div class="flex items-center gap-2 mb-3">
          <span class="text-2xl">🇪🇸</span>
          <span class="text-base font-semibold text-slate-800">España</span>
        </div>
        <div class="flex items-center justify-between text-xs text-slate-400 mb-1.5">
          <span>0 días acumulados</span>
          <span>183 días límite</span>
        </div>
        <div class="w-full bg-slate-100 rounded-full h-1.5">
          <div class="bg-amber-400 h-1.5 rounded-full" style="width: 0%"></div>
        </div>
        <p class="text-xs text-amber-600 mt-2 flex items-center gap-1">
          <svg class="w-3 h-3 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
          </svg>
          Añade billetes para calcular el riesgo
        </p>
      </div>

      <%# Card 3 — Países Visitados (indigo) %>
      <div class="bg-white rounded-xl border border-slate-200 shadow-sm p-5">
        <div class="flex items-center justify-between mb-3">
          <span class="text-xs font-bold text-slate-400 uppercase tracking-wider">Países Visitados</span>
          <span class="text-2xl font-bold text-indigo-600">0</span>
        </div>
        <p class="text-sm text-slate-300 mt-2">Aún no hay países registrados.</p>
        <p class="text-xs text-slate-400 mt-3">Sube tus billetes para ver el mapa.</p>
      </div>

    </div>
    <!-- /PLACEHOLDER -->

    <%# --- 2b. BANNER: billetes pendientes de análisis (siempre existe para Turbo Stream replace) --- %>
    <%= render "dashboard/pending_analysis_banner" %>

    <%# --- 3. BILLETES SUBIDOS --- %>
    <% if @tickets.any? %>
      <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden mb-6">

        <div id="tickets_section_header"
             class="px-6 py-4 bg-slate-50/50 border-b border-slate-200 flex items-center justify-between">
          <h2 class="text-xs font-bold text-slate-400 uppercase tracking-wider">Billetes Subidos</h2>
          <span class="text-xs text-slate-400">Pendientes de procesar</span>
        </div>

        <div id="tickets_list">
          <%= render @tickets %>
        </div>

      </div>
    <% else %>
      <%# Sección vacía — los Turbo Streams actualizan estos IDs aunque estén ocultos %>
      <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden mb-6 hidden"
           id="tickets_empty_section">
        <div id="tickets_section_header"></div>
        <div id="tickets_list"></div>
      </div>
    <% end %>

    <%# --- 4. TABLA DE VIAJES --- %>
    <div class="bg-white border border-slate-200 rounded-xl shadow-sm overflow-hidden">

      <div class="px-6 py-4 bg-slate-50/50 border-b border-slate-200 flex items-center justify-between">
        <h2 class="text-xs font-bold text-slate-400 uppercase tracking-wider">Mis Viajes</h2>
      </div>

      <% if @trips&.any? %>
        <div class="overflow-x-auto">
          <table class="w-full text-sm text-slate-600">
            <thead>
              <tr class="border-b border-slate-100">
                <th class="pl-4 pr-2 py-3 w-14"></th>
                <th class="px-6 py-3 text-left text-xs font-bold text-slate-400 uppercase tracking-wider">Destino</th>
                <th class="px-6 py-3 text-left text-xs font-bold text-slate-400 uppercase tracking-wider">País</th>
                <th class="px-6 py-3 text-left text-xs font-bold text-slate-400 uppercase tracking-wider">Inicio</th>
                <th class="px-6 py-3 text-left text-xs font-bold text-slate-400 uppercase tracking-wider">Fin</th>
                <th class="px-6 py-3 text-left text-xs font-bold text-slate-400 uppercase tracking-wider">Billetes</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-slate-100">
              <% @trips.each do |trip| %>
                <tr class="hover:bg-slate-50 transition-colors">
                  <td class="pl-4 pr-2 py-4">
                    <div class="w-10 h-10 rounded-full bg-slate-100 border border-slate-200
                                flex items-center justify-center text-xl shadow-sm flex-shrink-0">
                      <%= flag_emoji(trip.country) %>
                    </div>
                  </td>
                  <td class="px-6 py-4 font-medium text-slate-900"><%= trip.destination %></td>
                  <td class="px-6 py-4 text-slate-600"><%= trip.country %></td>
                  <td class="px-6 py-4 text-slate-600"><%= trip.start_date&.strftime("%d %b %Y") %></td>
                  <td class="px-6 py-4 text-slate-600"><%= trip.end_date&.strftime("%d %b %Y") %></td>
                  <td class="px-6 py-4">
                    <% if trip.tickets.attached? %>
                      <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-emerald-100 text-emerald-700">
                        <%= trip.tickets.count %> archivo(s)
                      </span>
                    <% else %>
                      <span class="text-slate-300">—</span>
                    <% end %>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>

      <% else %>
        <%# Estado vacío %>
        <div class="flex flex-col items-center justify-center py-16 text-center px-6">
          <svg class="w-20 h-20 text-slate-200 mb-5" fill="none" viewBox="0 0 24 24"
               stroke="currentColor" stroke-width="0.7">
            <path stroke-linecap="round" stroke-linejoin="round"
                  d="M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
          </svg>
          <p class="text-base font-semibold text-slate-500 mb-1">No tienes viajes registrados.</p>
          <p class="text-sm text-slate-400 mb-6">Sube tus billetes para empezar a organizar tu diario.</p>
          <%= link_to "Subir tu primer billete",
                new_ticket_path,
                data: { turbo_frame: "modal" },
                class: "border border-slate-200 text-slate-600 rounded-lg px-4 py-2 text-sm
                        font-medium hover:bg-slate-50 transition-colors" %>
        </div>
      <% end %>

    </div>
    <%# /Tabla de Viajes %>

  </div>
  <%# /Contenido principal %>

</div>
<%# /min-h-screen %>

<%# === TURBO FRAME PARA EL MODAL (inicialmente vacío) === %>
<%= turbo_frame_tag "modal", data: { controller: "modal" } %>
