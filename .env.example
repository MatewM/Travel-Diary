# ============================================================
# Travel Diary - Variables de entorno
# ============================================================
# Copia este archivo a .env y completa con tus valores REALES.
# NUNCA subas .env al repositorio (ya está en .gitignore).
#
# Para generar claves de encriptación: rails db:encryption:init
# ============================================================

# PostgreSQL (opcional si usas socket local)
# DATABASE_URL=postgresql://user:password@localhost/travel_diary_development

# ---- OAuth Google ----
# Obtén credenciales en: https://console.cloud.google.com/apis/credentials
# Tipo: Aplicación web
# URIs de redirección autorizados: http://localhost:3000/users/auth/google_oauth2/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ---- OAuth Apple (opcional) ----
# Requiere cuenta Apple Developer
APPLE_CLIENT_ID=
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY=

# ---- Encriptación de tokens OAuth ----
# Ejecuta: bin/rails db:encryption:init para generar estas claves
ACTIVE_RECORD_ENCRYPTION_PRIMARY_KEY=
ACTIVE_RECORD_ENCRYPTION_DETERMINISTIC_KEY=
ACTIVE_RECORD_ENCRYPTION_KEY_DERIVATION_SALT=
